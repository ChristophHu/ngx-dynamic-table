import { IdToValuePipe } from './id-to-value.pipe';

describe('IdToValuePipe', () => {
  it('create an instance', () => {
    const pipe = new IdToValuePipe();
    expect(pipe).toBeTruthy();
  });
});
