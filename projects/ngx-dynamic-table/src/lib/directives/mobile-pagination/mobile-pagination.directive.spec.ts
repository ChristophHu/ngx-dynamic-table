import { MobilePaginationDirective } from './mobile-pagination.directive'

describe('MobilePaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilePaginationDirective()
    expect(directive).toBeTruthy()
  });
});
